import type { QAItem } from '../interview-data'

// Câu hỏi phỏng vấn (Middle Frontend) + Câu hỏi bên lề
export const FPT_EXTRA_DATA: QAItem[] = [
  // === ReactJS Middle (801-830) ===
  { id: 801, category: "React", subcategory: "Hooks & State", level: "intermediate", q: "Giải thích sự khác nhau giữa useState và useReducer? Khi nào nên dùng useReducer?", a: "useState phù hợp cho state đơn giản (boolean, string, number). useReducer cho state phức tạp có nhiều sub-values hoặc khi next state phụ thuộc previous state. useReducer hay dùng cho form phức tạp, wizard multi-step, hoặc khi state logic cần tách riêng để test." },
  { id: 802, category: "React", subcategory: "Hooks & State", level: "intermediate", q: "useMemo và useCallback khác nhau thế nào? Cho ví dụ thực tế?", a: "useMemo cache kết quả tính toán: `useMemo(() => expensiveCalc(data), [data])`. useCallback cache function reference: `useCallback(() => handleClick(id), [id])`. useCallback dùng khi truyền callback xuống child component dùng React.memo. Lạm dụng 2 hooks này có thể giảm performance do overhead." },
  { id: 803, category: "React", subcategory: "Hooks & State", level: "intermediate", q: "useEffect cleanup function chạy khi nào? Tại sao quan trọng?", a: "Cleanup chạy: (1) trước mỗi lần effect re-run khi dependencies thay đổi, (2) khi component unmount. Quan trọng để tránh memory leaks: hủy subscriptions, abort fetch requests, clear timers. Ví dụ: `return () => controller.abort()` trong fetch effect." },
  { id: 804, category: "React", subcategory: "Hooks & State", level: "intermediate", q: "Custom hooks là gì? Viết custom hook useFetch đơn giản?", a: "Custom hooks là functions bắt đầu bằng 'use', tái sử dụng logic có stateful giữa components. useFetch: `function useFetch(url) { const [data, setData] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { fetch(url).then(r => r.json()).then(setData).finally(() => setLoading(false)); }, [url]); return { data, loading }; }`" },
  { id: 805, category: "React", subcategory: "Performance", level: "intermediate", q: "React.memo dùng khi nào? Khi nào KHÔNG nên dùng?", a: "Dùng khi component nhận props ổn định, render tốn kém, và parent re-render thường xuyên mà props không đổi. KHÔNG dùng khi: component luôn nhận props mới (objects/arrays tạo mới), component render nhẹ, hoặc dùng inline callbacks (phá vỡ memo)." },
  { id: 806, category: "React", subcategory: "Performance", level: "intermediate", q: "Tại sao không nên tạo object/array mới trong JSX props?", a: "`<Child style={{color: 'red'}}/>` tạo object mới mỗi render → Child luôn re-render dù dùng React.memo. Fix: khai báo object ngoài component hoặc useMemo. Tương tự với `onClick={() => handle(id)}` → dùng useCallback hoặc data attribute." },
  { id: 807, category: "React", subcategory: "Performance", level: "advanced", q: "Code splitting trong React thực hiện thế nào? Lazy loading routes?", a: "`React.lazy(() => import('./HeavyComponent'))` + `<Suspense fallback={<Loading/>}>`. Cho routes: lazy load từng page component. Giảm initial bundle size đáng kể. Kết hợp Webpack/Vite code splitting tự động tại chunk boundaries." },
  { id: 808, category: "State Management", subcategory: "State Management", level: "intermediate", q: "So sánh các thư viện quản lý state phổ biến: Redux Toolkit, Zustand, Context API, Jotai? Khi nào dùng gì?", a: "Context API: state đơn giản, ít thay đổi (theme, auth, locale) — không cần cài thêm gì. Zustand: nhẹ (~1KB), ít boilerplate, API đơn giản, phù hợp đa số projects. Redux Toolkit: enterprise, complex state cần middleware (async thunks, saga), DevTools mạnh, team lớn cần convention rõ ràng. Jotai: atomic state (mỗi state là 1 atom độc lập), tối ưu re-render vì chỉ component dùng atom đó mới update. Lưu ý: Recoil (Meta) đã ngừng phát triển — nên chuyển sang Jotai. Thực tế hiện tại: Zustand phổ biến nhất cho project mới vì đơn giản và đủ mạnh." },
  { id: 809, category: "State Management", subcategory: "State Management", level: "intermediate", q: "Redux Toolkit createSlice hoạt động thế nào?", a: "createSlice kết hợp action creators + reducer: `createSlice({ name: 'user', initialState, reducers: { setUser(state, action) { state.name = action.payload } } })`. Tự tạo action types, dùng Immer nên mutate state trực tiếp. Export actions và reducer." },
  { id: 810, category: "React", subcategory: "Patterns", level: "intermediate", q: "Compound Components pattern là gì? Ví dụ?", a: "Components chia sẻ state ngầm qua Context. Ví dụ: `<Tabs><Tabs.List><Tabs.Tab>A</Tabs.Tab></Tabs.List><Tabs.Panel>Content A</Tabs.Panel></Tabs>`. Flexible API, user kiểm soát layout. Dùng cho: Tabs, Accordion, Dropdown, Select." },
  { id: 811, category: "React", subcategory: "Patterns", level: "advanced", q: "Error Boundaries hoạt động thế nào? Hạn chế gì?", a: "Class component dùng `componentDidCatch` và `getDerivedStateFromError` bắt lỗi render. Hiển thị fallback UI thay vì crash toàn app. Hạn chế: không bắt lỗi trong event handlers, async code, SSR, và chính Error Boundary. Dùng react-error-boundary library cho function components." },
  { id: 812, category: "Testing", subcategory: "Testing", level: "intermediate", q: "Testing React components thường dùng gì?", a: "Bộ công cụ phổ biến nhất để test React components là Jest kết hợp React Testing Library (RTL), trong đó RTL theo triết lý test hành vi người dùng thay vì chi tiết implementation bên trong. Cách viết test cơ bản là dùng `render()` để mount component, `screen.getByRole()` hoặc `screen.getByText()` để tìm element, rồi `userEvent.click()` để mô phỏng tương tác. Để mock API calls, nên dùng MSW (Mock Service Worker) vì nó chặn requests ở tầng network, giúp test giống thực tế hơn so với mock trực tiếp fetch/axios. Nguyên tắc quan trọng là viết test theo luồng tương tác của user thực — ví dụ click nút submit rồi kiểm tra hiển thị thông báo thành công, thay vì kiểm tra state nội bộ có thay đổi hay không." },
  { id: 813, category: "React", subcategory: "Architecture", level: "intermediate", q: "Cấu trúc folder React project thường tổ chức thế nào?", a: "Cách tổ chức phổ biến và hiệu quả nhất là theo feature-based, trong đó mỗi tính năng có folder riêng như `src/features/auth/` hay `src/features/dashboard/`, bên trong chứa đầy đủ components, hooks, services, và types của tính năng đó. Phần dùng chung được đặt ở `src/components/` cho UI components, `src/hooks/` cho custom hooks, `src/utils/` cho helper functions, và `src/services/` cho API calls. Mỗi folder nên có file `index.ts` làm barrel export để import gọn gàng hơn. Cách tổ chức này tốt hơn nhiều so với cấu trúc theo type (gom tất cả components vào một folder, tất cả hooks vào một folder) vì khi dự án lớn lên, feature-based giúp tìm và sửa code liên quan đến một tính năng dễ dàng hơn rất nhiều." },
  { id: 814, category: "React", subcategory: "Architecture", level: "advanced", q: "Giải thích React Server Components vs Client Components?", a: "Server Components render trên server, gửi HTML + data (không JS). Giảm bundle size, truy cập DB trực tiếp. Client Components render trên browser, có interactivity (hooks, events). Mặc định là Server trong Next.js App Router. Thêm `'use client'` cho interactivity." },
  { id: 815, category: "React", subcategory: "Architecture", level: "advanced", q: "Virtual DOM và Reconciliation trong React hoạt động thế nào?", a: "React tạo Virtual DOM (JS object) đại diện UI. Khi state thay đổi, tạo VDOM mới, so sánh (diffing) với VDOM cũ, chỉ cập nhật DOM thật phần khác biệt. Diffing O(n) nhờ: so sánh cùng level, key prop cho lists, khác type = rebuild." },

  // === NodeJS Cơ Bản (831-850) ===
  { id: 831, category: "Node.js", subcategory: "Cơ Bản", level: "beginner", q: "Node.js là gì? Tại sao dùng cho backend?", a: "Runtime JavaScript trên server, dùng V8 engine. Ưu điểm: non-blocking I/O (xử lý nhiều requests đồng thời), cùng ngôn ngữ với frontend (JS), npm ecosystem khổng lồ, tốt cho real-time apps. Thường dùng Node.js cho API servers, microservices, BFF (Backend for Frontend)." },
  { id: 832, category: "Node.js", subcategory: "Cơ Bản", level: "beginner", q: "Event Loop trong Node.js hoạt động thế nào?", a: "Single-threaded event loop xử lý async operations. Quy trình: Call Stack → Node APIs (I/O, timers) → Callback Queue → Event Loop kiểm tra Stack trống → đẩy callback lên Stack. Phases: timers → I/O callbacks → idle → poll → check → close. Microtasks (Promises) chạy giữa mỗi phase." },
  { id: 833, category: "Node.js", subcategory: "Cơ Bản", level: "beginner", q: "require() và import khác nhau thế nào?", a: "`require()` là CommonJS (Node.js truyền thống): sync loading, dynamic imports, `module.exports`. `import` là ES Modules: async, static analysis (tree shaking), `export default/named`. Node.js hỗ trợ cả hai nhưng ES Modules là tương lai. Dùng .mjs hoặc `\"type\": \"module\"` trong package.json." },
  { id: 834, category: "Node.js", subcategory: "Express", level: "beginner", q: "Middleware trong Express là gì? Cho ví dụ?", a: "Function nhận (req, res, next), xử lý request trước khi đến route handler. Ví dụ: `app.use(cors())`, `app.use(express.json())`, auth middleware kiểm tra JWT. Gọi `next()` để chuyển sang middleware tiếp theo. Thứ tự middleware quan trọng." },
  { id: 835, category: "Node.js", subcategory: "Express", level: "intermediate", q: "REST API là gì? Thiết kế REST API cần tuân thủ những nguyên tắc nào?", a: "REST (Representational State Transfer) là kiến trúc thiết kế API dựa trên HTTP, trong đó mỗi URL đại diện cho một resource (tài nguyên). Nguyên tắc quan trọng: dùng danh từ số nhiều cho endpoints (/users, /products), dùng đúng HTTP methods — GET lấy dữ liệu, POST tạo mới, PUT/PATCH cập nhật, DELETE xóa. Status codes phải chính xác: 200 thành công, 201 tạo mới OK, 400 request sai, 401 chưa đăng nhập, 404 không tìm thấy, 500 lỗi server. Ngoài ra cần versioning (/api/v1/), pagination (?page=1&limit=20), và response format thống nhất dạng `{ data, error, meta }` để frontend dễ xử lý." },
  { id: 836, category: "Node.js", subcategory: "Express", level: "intermediate", q: "Error handling trong Express thế nào?", a: "Express xử lý lỗi thông qua error middleware đặc biệt có 4 tham số `(err, req, res, next)`, được đặt cuối cùng sau tất cả routes để bắt mọi lỗi phát sinh. Với async route handlers, cần bọc code trong try/catch hoặc dùng thư viện express-async-errors để tự động forward lỗi async đến error middleware mà không cần try/catch thủ công. Nên tạo custom error classes kế thừa từ Error như AppError với thuộc tính status code riêng, giúp phân biệt lỗi validation (400), unauthorized (401), hay server error (500). Điều quan trọng là luôn log errors đầy đủ ở server side nhưng chỉ trả về message an toàn cho client, tránh lộ thông tin nhạy cảm như stack trace hay database query." },
  { id: 837, category: "Security", subcategory: "Auth", level: "intermediate", q: "JWT authentication flow trong Node.js?", a: "Login: verify credentials → tạo JWT (sign với secret) → gửi token cho client. Request: client gửi token trong Authorization header (Bearer token). Server: middleware verify token, extract user info, attach vào req.user. Lưu trữ: access token → memory (JS variable), refresh token → httpOnly cookie. KHÔNG lưu localStorage (XSS risk). Refresh token cho long-lived sessions." },
  { id: 838, category: "Security", subcategory: "Auth", level: "intermediate", q: "Bcrypt dùng để làm gì? Tại sao không hash bằng MD5/SHA?", a: "Bcrypt hash passwords với salt tự động và cost factor (làm chậm brute-force). MD5/SHA không phù hợp vì quá nhanh (~billions hashes/sec) → brute-force trivial ngay cả khi có salt. `bcrypt.hash(password, 10)` (10 = salt rounds). `bcrypt.compare(password, hash)` để verify. Alternatives: argon2 (mới hơn, mạnh hơn)." },
  { id: 839, category: "Database", subcategory: "Database", level: "intermediate", q: "Prisma ORM là gì? Tại sao phổ biến?", a: "Type-safe ORM cho Node.js/TypeScript. Schema-first: định nghĩa models trong schema.prisma → generate client. Auto-complete, type checking cho queries. Migration tự động. Hỗ trợ PostgreSQL, MySQL, SQLite, MongoDB. Dự án enterprise mới thường chọn Prisma thay Sequelize." },
  { id: 840, category: "Node.js", subcategory: "Advanced", level: "intermediate", q: "Cách handle file upload trong Node.js?", a: "Để xử lý file upload trong Express, thư viện phổ biến nhất là multer hoạt động như một middleware, cấu hình bằng `const upload = multer({ dest: 'uploads/' })` rồi gắn vào route cụ thể như `app.post('/upload', upload.single('file'), handler)`. Khi nhận file cần validate kỹ hai thứ: kích thước file (giới hạn bằng option `limits: { fileSize: 5 * 1024 * 1024 }` cho 5MB) và loại file qua mimetype để chặn file độc hại. Trong môi trường production, không nên lưu file vào server local mà upload thẳng lên cloud storage như AWS S3 hoặc Google Cloud Storage để đảm bảo scalability và reliability. Với file lớn hàng trăm MB trở lên, nên dùng stream upload thay vì đọc toàn bộ file vào memory để tránh crash server." },

  // === System Design / Kiến trúc (871-875) ===
  { id: 871, category: "React", subcategory: "Frontend", level: "advanced", q: "Thiết kế hệ thống chat real-time như thế nào?", a: "Hệ thống chat real-time cần dùng WebSocket để giao tiếp hai chiều giữa client và server, thay vì HTTP polling vì WebSocket giữ kết nối mở liên tục và có độ trễ thấp hơn nhiều. Phía frontend gồm các components chính: MessageList (dùng virtualized list để hiển thị hàng nghìn tin nhắn mượt mà), MessageInput, và ChatSidebar hiển thị danh sách rooms. State management nên dùng Zustand hoặc Redux để quản lý messages và online status, kết hợp Optimistic UI hiển thị tin nhắn ngay lập tức trước khi server confirm để UX mượt hơn. Cần có chiến lược reconnection tự động khi mất kết nối, lazy load tin nhắn cũ khi scroll lên, và dùng IndexedDB để cache messages offline giúp app vẫn hoạt động khi không có mạng." },
  { id: 872, category: "React", subcategory: "Frontend", level: "advanced", q: "Thiết kế infinite scroll feed (như Facebook/Twitter)?", a: "Infinite scroll feed cần kết hợp nhiều kỹ thuật để vừa mượt vừa hiệu quả. Dùng Intersection Observer gắn vào một sentinel element ở cuối danh sách, khi element này xuất hiện trong viewport thì tự động trigger load thêm dữ liệu. Để hiển thị hàng nghìn items mà không lag, dùng virtual list với react-window hoặc @tanstack/virtual để chỉ render những items đang nhìn thấy trên màn hình. Về phía API, nên dùng cursor-based pagination thay vì offset vì cursor không bị trùng lặp data khi có bài viết mới, kết hợp React Query useInfiniteQuery để quản lý cache và fetch states. Ngoài ra cần xử lý restore scroll position khi user navigate đi rồi quay lại, hiển thị skeleton loading cho UX tốt hơn, và debounce scroll events để tránh gọi API quá nhiều lần." },
  { id: 873, category: "React", subcategory: "Frontend", level: "advanced", q: "Thiết kế form builder (drag & drop)?", a: "Form builder cần hai chế độ chính: Edit mode cho phép kéo thả các field types vào canvas, và Preview mode hiển thị form như user cuối sẽ thấy. Phần drag and drop nên dùng thư viện @dnd-kit vì nó nhẹ, accessible, và hỗ trợ tốt React — hoặc react-beautiful-dnd nếu cần animation đẹp hơn. Kiến trúc cốt lõi là schema-driven: mỗi form được biểu diễn dưới dạng JSON schema, và có một component registry để map từ field type sang React component tương ứng, giúp dễ mở rộng thêm loại field mới. Các tính năng nâng cao gồm validation rules engine cho phép config validation mỗi field, undo/redo dùng command pattern để lưu lịch sử thao tác, và khả năng export form data cùng generate endpoint nhận submissions." },
  { id: 875, category: "React", subcategory: "Frontend", level: "intermediate", q: "Caching strategies cho frontend app?", a: "Có nhiều tầng caching cho frontend application, mỗi tầng phục vụ mục đích khác nhau. Tầng trình duyệt dùng HTTP headers như Cache-Control và ETag để cache tài nguyên tĩnh (JS, CSS, images), giúp giảm network requests khi user quay lại trang. Tầng ứng dụng dùng React Query hoặc SWR với chiến lược stale-while-revalidate để cache dữ liệu API — hiển thị data cũ ngay lập tức rồi cập nhật phía sau, mang lại trải nghiệm nhanh cho user. Service Worker cho phép xây dựng ứng dụng offline-first bằng cách cache resources trong Cache API. Ngoài ra còn localStorage cho user preferences, SessionStorage cho form drafts tạm thời, IndexedDB cho tập dữ liệu lớn cần truy vấn, và CDN cho static assets giúp giảm latency theo vùng địa lý." },

  // === Câu hỏi thường gặp khác (876-900) ===
  { id: 876, category: "Build Tools", subcategory: "Git", level: "beginner", q: "Git Flow là gì? Có những branching strategy nào phổ biến?", a: "Git Flow có 5 loại branch: `main` (production), `develop` (tích hợp code), `feature/*` (tính năng mới), `release/*` (chuẩn bị release), `hotfix/*` (sửa lỗi khẩn cấp). Trunk-based Development là hướng ngược lại: commit thẳng vào `main` với feature flags, phù hợp team CI/CD mature. Thực tế phổ biến nhất: feature branch từ develop → PR review → merge develop → staging → merge main → production. Naming convention quan trọng: `feature/JIRA-123-add-login` giúp trace thay đổi về requirement." },
  { id: 877, category: "Build Tools", subcategory: "Git", level: "intermediate", q: "Git rebase vs merge khác nhau? Khi nào dùng gì?", a: "Merge: tạo merge commit, giữ history đầy đủ, safe cho shared branches. Rebase: rewrite history thành linear, clean hơn, KHÔNG dùng cho shared branches. Best practice: rebase feature branch lên develop trước khi merge (hoặc squash merge). `git rebase -i` để clean commits." },
  { id: 882, category: "Build Tools", subcategory: "DevOps", level: "intermediate", q: "CI/CD pipeline là gì? Frontend project thường cấu hình pipeline gồm những bước nào?", a: "CI/CD (Continuous Integration / Continuous Deployment) là quy trình tự động kiểm tra và deploy code mỗi khi có thay đổi. Pipeline frontend chạy tuần tự để fail-fast: (1) lint + format check (nhanh nhất), (2) type check `tsc --noEmit`, (3) unit tests với coverage threshold, (4) build production. Ở tầng staging: deploy preview mỗi PR (Vercel preview URLs rất tiện), chạy E2E tests với Playwright. Deploy production khi merge vào main. Công cụ phổ biến: GitHub Actions (miễn phí cho open source, tích hợp sẵn GitHub), GitLab CI, hoặc Jenkins/Azure DevOps cho enterprise on-premise." },
  { id: 883, category: "Build Tools", subcategory: "DevOps", level: "intermediate", q: "Docker là gì? Frontend developer cần biết gì về Docker?", a: "Docker đóng gói ứng dụng và dependencies vào container, đảm bảo chạy giống nhau trên mọi máy (dev, staging, production). Frontend dev cần biết: (1) Dockerfile multi-stage — stage 1 build app, stage 2 chỉ copy file build sang nginx/node, giảm image từ 1GB xuống ~100MB. (2) docker-compose.yml để chạy frontend + backend + database cùng lúc bằng `docker-compose up`. (3) .dockerignore loại `node_modules` và `.next` tránh copy file thừa. Lưu ý quan trọng: environment variables phải inject lúc runtime (`docker run -e`), không hardcode vào image vì image dùng chung cho nhiều môi trường." },
  { id: 884, category: "Performance", subcategory: "Web Perf", level: "intermediate", q: "Lighthouse là gì? Làm sao cải thiện điểm Lighthouse cho website?", a: "Lighthouse là công cụ của Google đánh giá website theo 4 tiêu chí. Performance: lazy load images (`loading='lazy'`), code splitting với React.lazy, compress assets (gzip/brotli), preload fonts quan trọng. Accessibility: mọi image có alt text, color contrast đủ ratio, dùng ARIA cho interactive elements. Best Practices: dùng HTTPS, không console errors, không deprecated APIs. SEO: meta tags đầy đủ (title, description), semantic HTML (h1, nav, main), sitemap.xml. Mục tiêu: Performance > 90, Accessibility > 90. Đo bằng Chrome DevTools tab Lighthouse hoặc PageSpeed Insights." },
  { id: 885, category: "Performance", subcategory: "Web Perf", level: "intermediate", q: "Web Vitals (LCP, CLS, INP) tối ưu thế nào?", a: "Web Vitals là ba chỉ số chính mà Google dùng để đánh giá trải nghiệm người dùng thực tế trên website. LCP (Largest Contentful Paint) đo thời gian hiển thị phần tử lớn nhất, cần dưới 2.5 giây — tối ưu bằng cách preload hero image, inline critical CSS, và giảm server response time. CLS (Cumulative Layout Shift) đo mức độ nhảy layout, cần dưới 0.1 — khắc phục bằng cách luôn set width/height cho images, reserve space cho quảng cáo và dynamic content, dùng font-display: swap để tránh font gây layout shift. INP (Interaction to Next Paint) đo độ phản hồi khi user tương tác, cần dưới 200ms — cải thiện bằng cách break long tasks thành chunks nhỏ hơn, defer non-critical JavaScript, và dùng Web Workers cho tính toán nặng. Đo các chỉ số này bằng Chrome DevTools tab Performance hoặc thư viện web-vitals trong code." },
  { id: 886, category: "Security", subcategory: "Security", level: "intermediate", q: "Frontend security best practices?", a: "Bảo mật frontend cần chú ý nhiều lớp phòng thủ khác nhau. Chống XSS (Cross-Site Scripting) bằng cách luôn sanitize user input, dùng textContent thay vì innerHTML để tránh inject script, và thiết lập Content Security Policy (CSP) headers trên server. Chống CSRF (Cross-Site Request Forgery) bằng SameSite cookies và CSRF tokens gửi kèm mỗi request có side effects. Về authentication, lưu tokens trong httpOnly cookies thay vì localStorage vì localStorage dễ bị đọc qua XSS, và không bao giờ lưu sensitive data ở client side. Cần thường xuyên chạy `npm audit` hoặc dùng Snyk để phát hiện vulnerabilities trong dependencies, cấu hình CORS chỉ whitelist các origins cần thiết, và bắt buộc HTTPS cho toàn bộ ứng dụng." },

  // === BỔ SUNG: Coding Challenges thường hỏi (889-904) ===
  { id: 889, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Viết hàm debounce từ đầu (không dùng lodash)?", a: "`function debounce(fn, delay) { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), delay); }; }` Ứng dụng: search input (gõ xong 300ms mới gọi API), resize handler, auto-save. Phỏng vấn thường hỏi viết tay để kiểm tra hiểu closure và timer." },
  { id: 890, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Viết hàm throttle từ đầu?", a: "`function throttle(fn, limit) { let inThrottle; return (...args) => { if (!inThrottle) { fn(...args); inThrottle = true; setTimeout(() => inThrottle = false, limit); } }; }` Khác debounce: throttle gọi ngay lần đầu rồi chặn, debounce đợi hết delay mới gọi. Dùng cho scroll, mousemove." },
  { id: 891, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Implement deep clone object (không dùng structuredClone)?", a: "Recursive: `function deepClone(obj) { if (obj === null || typeof obj !== 'object') return obj; const clone = Array.isArray(obj) ? [] : {}; for (const key in obj) { if (obj.hasOwnProperty(key)) clone[key] = deepClone(obj[key]); } return clone; }` Lưu ý: không handle Date, RegExp, Map, Set, circular refs. Production dùng `structuredClone()` hoặc lodash." },
  { id: 892, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Flatten nested array (không dùng Array.flat)?", a: "Recursive: `function flatten(arr) { return arr.reduce((acc, item) => acc.concat(Array.isArray(item) ? flatten(item) : item), []); }` Hoặc iterative với stack: `while (arr.some(Array.isArray)) arr = [].concat(...arr);` Phỏng vấn thường hỏi để test hiểu recursion và Array methods." },
  { id: 893, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Implement Promise.all từ đầu?", a: "`function promiseAll(promises) { return new Promise((resolve, reject) => { if (promises.length === 0) { resolve([]); return; } const results = []; let count = 0; promises.forEach((p, i) => { Promise.resolve(p).then(val => { results[i] = val; if (++count === promises.length) resolve(results); }).catch(reject); }); }); }` Lưu ý: handle empty array (resolve ngay []). Reject ngay khi 1 promise reject." },
  { id: 894, category: "JavaScript", subcategory: "Coding Challenge", level: "advanced", q: "Implement EventEmitter class đơn giản?", a: "`class EventEmitter { constructor() { this.events = {}; } on(event, fn) { (this.events[event] ||= []).push(fn); } off(event, fn) { this.events[event] = this.events[event]?.filter(f => f !== fn); } emit(event, ...args) { this.events[event]?.forEach(fn => fn(...args)); } }` Pattern Observer, dùng trong state management, pub/sub systems." },
  { id: 895, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Viết hook useLocalStorage trong React?", a: "`function useLocalStorage(key, initial) { const [value, setValue] = useState(() => { try { const item = localStorage.getItem(key); return item ? JSON.parse(item) : initial; } catch { return initial; } }); useEffect(() => { localStorage.setItem(key, JSON.stringify(value)); }, [key, value]); return [value, setValue]; }` Test khả năng viết custom hooks." },
  { id: 896, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Implement infinite scroll hook?", a: "Dùng IntersectionObserver: `function useInfiniteScroll(callback) { const ref = useRef(null); useEffect(() => { const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) callback(); }); if (ref.current) observer.observe(ref.current); return () => observer.disconnect(); }, [callback]); return ref; }` Gán ref cho sentinel element cuối danh sách." },
  { id: 897, category: "JavaScript", subcategory: "Coding Challenge", level: "advanced", q: "Viết hàm curry đa năng?", a: "Currying là kỹ thuật biến một hàm nhận nhiều tham số thành chuỗi các hàm mỗi hàm nhận một hoặc vài tham số. Cách implement: `function curry(fn) { return function curried(...args) { if (args.length >= fn.length) return fn(...args); return (...more) => curried(...args, ...more); }; }` — hàm kiểm tra nếu đủ số tham số thì gọi hàm gốc, chưa đủ thì trả về hàm mới chờ nhận thêm. Ví dụ: `const add = curry((a, b, c) => a + b + c)` cho phép gọi linh hoạt `add(1)(2)(3)`, `add(1, 2)(3)`, hay `add(1, 2, 3)` đều trả về 6. Đây là câu hỏi phỏng vấn kiểm tra kiến thức functional programming, closures, và khả năng xử lý arguments linh hoạt." },
  { id: 898, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Implement simple React context store (mini Redux)?", a: "Tạo context + reducer: `const StoreContext = createContext(); function StoreProvider({ children }) { const [state, dispatch] = useReducer(reducer, initialState); return <StoreContext.Provider value={{ state, dispatch }}>{children}</StoreContext.Provider>; }` Kèm `useStore = () => useContext(StoreContext)`. Phỏng vấn thường hỏi để test hiểu state management internals." },
  { id: 899, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Viết hàm retry với exponential backoff?", a: "`async function retry(fn, maxRetries = 3, delay = 1000) { for (let i = 0; i < maxRetries; i++) { try { return await fn(); } catch (err) { if (i === maxRetries - 1) throw err; await new Promise(r => setTimeout(r, delay * 2 ** i)); } } }` Dùng cho API calls không ổn định. Delay tăng: 1s, 2s, 4s." },
  { id: 900, category: "JavaScript", subcategory: "Coding Challenge", level: "advanced", q: "Implement simple virtual list (render chỉ visible items)?", a: "Virtual list là kỹ thuật chỉ render các items đang hiển thị trong viewport thay vì toàn bộ danh sách, giúp xử lý hàng chục nghìn items mà không lag. Cách tính: lấy `visibleStart = Math.floor(scrollTop / itemHeight)` và `visibleEnd = visibleStart + Math.ceil(containerHeight / itemHeight)` để xác định range items cần render. Container bên ngoài cần có tổng chiều cao bằng `totalItems * itemHeight` để giữ scrollbar đúng kích thước, còn mỗi item dùng `position: absolute` với `top = index * itemHeight` để đặt đúng vị trí. Trong thực tế, nên dùng thư viện react-window hoặc @tanstack/virtual vì chúng đã xử lý tốt các edge cases như dynamic item heights, overscan (render thêm vài items ngoài viewport cho smooth scroll), và scroll restoration." },
  { id: 901, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Viết function so sánh 2 objects deep equal?", a: "`function deepEqual(a, b) { if (a === b) return true; if (typeof a !== 'object' || typeof b !== 'object' || !a || !b) return false; const keysA = Object.keys(a), keysB = Object.keys(b); if (keysA.length !== keysB.length) return false; return keysA.every(key => deepEqual(a[key], b[key])); }` Phỏng vấn thường hỏi để test recursion và edge cases (null, arrays)." },
  { id: 902, category: "JavaScript", subcategory: "Coding Challenge", level: "intermediate", q: "Implement memoize function cho expensive computations?", a: "`function memoize(fn) { const cache = new Map(); return (...args) => { const key = JSON.stringify(args); if (cache.has(key)) return cache.get(key); const result = fn(...args); cache.set(key, result); return result; }; }` Lưu ý: JSON.stringify chậm cho args lớn. Production dùng WeakMap cho object args, LRU cache giới hạn size." },
  { id: 903, category: "JavaScript", subcategory: "Coding Challenge", level: "advanced", q: "Implement simple pub/sub system cho micro-frontends?", a: "`const bus = { subs: {}, subscribe(event, fn) { (this.subs[event] ||= []).push(fn); return () => { this.subs[event] = this.subs[event].filter(f => f !== fn); }; }, publish(event, data) { this.subs[event]?.forEach(fn => fn(data)); } };` Dùng cho cross-app communication. Thêm: typed events, wildcard, once listener." },
  { id: 904, category: "JavaScript", subcategory: "Coding Challenge", level: "advanced", q: "Viết middleware pipeline pattern (giống Express)?", a: "Middleware pipeline là pattern cho phép xâu chuỗi nhiều hàm xử lý tuần tự, mỗi hàm nhận context và hàm next() để chuyển sang middleware tiếp theo — giống cách Express và Koa xử lý HTTP requests. Implement cốt lõi: `function compose(...fns) { return (ctx, next) => { let i = -1; function dispatch(n) { if (n <= i) return Promise.reject('next() called multiple times'); i = n; const fn = fns[n] || next; if (!fn) return Promise.resolve(); return Promise.resolve(fn(ctx, () => dispatch(n + 1))); } return dispatch(0); }; }` — hàm compose nhận danh sách middlewares và trả về một hàm duy nhất gọi lần lượt từng middleware. Biến `i` dùng để detect nếu next() bị gọi nhiều lần trong cùng một middleware, tránh loop vô hạn. Đây là câu hỏi nâng cao test hiểu biết về async composition, recursion, và Promise chaining." },

  // === BỔ SUNG: Scenario-based / Debug Questions (905-916) ===
  { id: 905, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "Component re-render liên tục, làm sao debug và fix?", a: "(1) React DevTools Profiler: tìm component render nhiều. (2) Nguyên nhân phổ biến: tạo object/array mới trong render, missing dependency array useEffect, context value thay đổi. (3) Fix: React.memo, useMemo/useCallback, split context, move state xuống component con." },
  { id: 906, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "API call bị gọi 2 lần trong useEffect, tại sao?", a: "React 18 Strict Mode gọi useEffect 2 lần trong development để detect side effects. Fix: (1) Đây là behavior đúng, production chỉ gọi 1 lần. (2) Dùng cleanup function abort previous request. (3) Hoặc dùng React Query/SWR thay vì fetch trực tiếp trong useEffect." },
  { id: 907, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "State update nhưng UI không re-render, nguyên nhân?", a: "(1) Mutate object/array trực tiếp thay vì tạo copy mới (React dùng reference comparison). (2) State nằm trong stale closure. (3) Dùng ref thay vì state. Fix: `setState([...arr])` hoặc `setState({...obj, key: value})` tạo reference mới. Immer giúp tránh lỗi này." },
  { id: 908, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "Memory leak trong React component, cách phát hiện và fix?", a: "Phát hiện: Chrome DevTools > Memory tab, check heap snapshots. Nguyên nhân: (1) Quên cleanup subscriptions/timers trong useEffect. (2) Event listeners không remove. (3) Abort controller không cancel fetch. Fix: return cleanup function trong useEffect. Pattern: `const controller = new AbortController(); return () => controller.abort();`" },
  { id: 909, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "Trang load chậm, approach debug từ đầu?", a: "Khi trang web load chậm, cần debug có hệ thống từ tổng quan đến chi tiết. Bước đầu tiên mở Chrome DevTools tab Network để xem waterfall — tìm các file quá lớn, requests bị blocking, hoặc API response chậm. Tiếp theo chạy Lighthouse audit để nhận danh sách cụ thể các vấn đề cần khắc phục kèm mức độ ưu tiên. Sau đó dùng tab Performance để record và tìm long tasks (>50ms) gây block main thread, layout thrashing, hoặc excessive reflows. Cuối cùng chạy bundle analyzer (webpack-bundle-analyzer hoặc vite plugin) để phát hiện dependencies nặng có thể thay thế hoặc lazy load. Thứ tự fix nên theo impact: tối ưu critical rendering path trước, rồi code splitting, image optimization bằng format WebP/AVIF, và cuối cùng là caching strategy." },
  { id: 910, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "Form phức tạp (20+ fields), cách tổ chức state hiệu quả?", a: "Với form nhiều fields, giải pháp tối ưu nhất là dùng React Hook Form vì nó hoạt động theo cơ chế uncontrolled components, chỉ re-render field nào thay đổi thay vì toàn bộ form — performance tốt hơn đáng kể so với controlled approach. Kết hợp Zod schema để validate type-safe, cấu hình bằng pattern `useForm({ resolver: zodResolver(schema) })` và bọc trong FormProvider để các nested components truy cập form state dễ dàng. Nếu form quá dài nên chia thành sections hoặc multi-step wizard, mỗi step validate riêng trước khi cho chuyển sang step tiếp theo. Trong trường hợp cần logic state phức tạp hơn như conditional fields phụ thuộc lẫn nhau, có thể kết hợp thêm useReducer để quản lý các business rules." },
  { id: 911, category: "React", subcategory: "Debug & Scenario", level: "advanced", q: "Dự án cũ dùng Class Components, chiến lược migrate sang Hooks?", a: "(1) Không rewrite toàn bộ cùng lúc. (2) Bắt đầu từ components mới (100% hooks). (3) Khi sửa bug/thêm feature → convert class → function. (4) Extract logic thành custom hooks (reuse). (5) Class + hooks chung sống tốt, không cần migrate hết. (6) Ưu tiên convert: HOCs → custom hooks, lifecycle → useEffect." },
  { id: 912, category: "React", subcategory: "Debug & Scenario", level: "advanced", q: "Thiết kế authentication flow cho SPA (React)?", a: "Login: POST credentials → nhận access + refresh token. Lưu access token trong memory (biến JS), refresh token trong httpOnly cookie. Axios interceptor: (1) Attach access token mỗi request. (2) 401 → refresh token → retry. (3) Refresh fail → logout. Route guard: PrivateRoute component check auth state." },
  { id: 913, category: "React", subcategory: "Debug & Scenario", level: "advanced", q: "i18n (đa ngôn ngữ) là gì? Triển khai multi-language trong React project thế nào?", a: "i18n (internationalization) là kỹ thuật giúp app hỗ trợ nhiều ngôn ngữ. Cách làm: (1) Tách tất cả text ra file JSON theo ngôn ngữ (vi.json, en.json), không hardcode text trong component. (2) Dùng thư viện react-i18next hoặc next-intl (cho Next.js) — gọi `t('greeting')` thay vì viết 'Xin chào' trực tiếp. (3) Lazy load file ngôn ngữ để không tải hết cùng lúc. (4) Dùng Intl API có sẵn trong browser để format ngày tháng, số, tiền tệ theo locale. (5) Next.js hỗ trợ routing theo locale: `/en/about`, `/vi/about` qua middleware. next-intl hiện là lựa chọn phổ biến nhất cho Next.js App Router." },
  { id: 914, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "CORS error khi gọi API, cách xử lý?", a: "CORS (Cross-Origin Resource Sharing) là cơ chế bảo mật của browser ngăn chặn frontend gọi API từ domain khác nếu server không cho phép, ví dụ app chạy ở localhost:3000 gọi API ở localhost:8080 sẽ bị chặn. Cách fix đúng là backend thêm header `Access-Control-Allow-Origin` chỉ định domain được phép, hoặc dùng thư viện cors trong Express với whitelist origins cụ thể. Trong môi trường development, cấu hình proxy trong Vite (`server.proxy`) hoặc Next.js (`rewrites`) để requests đi qua cùng origin, tránh CORS hoàn toàn. Ở production, dùng API gateway hoặc reverse proxy như nginx để route requests, và tuyệt đối không dùng CORS disable browser extension vì nó chỉ tắt bảo mật phía client mà không giải quyết gốc vấn đề." },
  { id: 915, category: "React", subcategory: "Debug & Scenario", level: "intermediate", q: "SSR/SSG content khác client-side, hydration mismatch?", a: "Hydration mismatch xảy ra khi HTML render từ server khác với kết quả render lần đầu trên client, khiến React báo warning và có thể gây lỗi UI. Nguyên nhân phổ biến nhất là dùng browser-only APIs như window hay localStorage trong quá trình server render (server không có window), hoặc dùng Date/time mà timezone server khác client, hay random values cho mỗi lần render ra kết quả khác nhau. Cách fix: đặt code phụ thuộc browser vào useEffect vì nó chỉ chạy trên client, dùng dynamic import với option `ssr: false` cho component cần browser APIs, hoặc thêm `suppressHydrationWarning` cho trường hợp biết trước sẽ khác nhau như timestamp. Nguyên tắc quan trọng là HTML server trả về phải khớp chính xác với kết quả initial render trên client." },
  { id: 916, category: "React", subcategory: "Debug & Scenario", level: "advanced", q: "Optimize React app cho 10,000+ items trong danh sách?", a: "Để hiển thị danh sách hàng chục nghìn items mà không giật lag, giải pháp quan trọng nhất là dùng virtual list với react-window hoặc @tanstack/virtual, chỉ render khoảng 20-30 items đang hiển thị trên màn hình thay vì toàn bộ. Kết hợp thêm React.memo cho list item component để tránh re-render không cần thiết, và useMemo cho logic filter/sort để không tính toán lại mỗi lần render. Nếu có search/filter phức tạp trên tập dữ liệu lớn, nên chuyển sang Web Worker để xử lý ở background thread, tránh block main thread gây đơ UI. Mục tiêu là giữ mỗi frame render dưới 16ms (60fps), và nên đo bằng React DevTools Profiler hoặc Chrome Performance tab để xác nhận." },

  // === BỔ SUNG: Node.js Deep Dive (917-926) ===
  { id: 917, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Streams trong Node.js là gì? Tại sao cần dùng Streams thay vì đọc toàn bộ file?", a: "Streams xử lý data theo từng phần nhỏ (chunks) thay vì load toàn bộ vào memory — rất quan trọng khi xử lý file lớn (CSV hàng GB, video). Có 4 loại: Readable (đọc data, ví dụ fs.createReadStream), Writable (ghi data, ví dụ fs.createWriteStream), Duplex (vừa đọc vừa ghi, ví dụ TCP socket), Transform (biến đổi data trong quá trình truyền, ví dụ compression). Dùng `.pipe()` để nối streams: `fs.createReadStream('big.csv').pipe(transform).pipe(res)`. Nếu đọc file 2GB bằng `fs.readFile` sẽ cần 2GB RAM, nhưng Stream chỉ cần vài MB." },
  { id: 918, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Process.env và dotenv hoạt động thế nào?", a: "`process.env` là object chứa tất cả environment variables của hệ điều hành mà Node.js process có thể truy cập, ví dụ `process.env.DATABASE_URL` để lấy connection string. Thư viện dotenv đọc file `.env` ở root project và inject các biến vào `process.env` lúc khởi động app, giúp developer không cần set biến môi trường thủ công. Nguyên tắc quan trọng: file `.env` phải nằm trong `.gitignore` để không commit secrets lên repository, và tạo file `.env.example` chứa danh sách biến cần thiết (không có giá trị thật) làm template cho team. Nên validate tất cả env vars lúc app startup bằng thư viện như Zod hoặc envalid để crash sớm nếu thiếu biến, thay vì gặp lỗi runtime khó debug khi app đang chạy." },
  { id: 919, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Rate limiting API endpoint thực hiện thế nào?", a: "Rate limiting giới hạn số lượng requests một client có thể gửi trong khoảng thời gian nhất định, giúp bảo vệ server khỏi bị quá tải hoặc bị tấn công brute-force. Với Express, dùng thư viện express-rate-limit cấu hình nhanh như `rateLimit({ windowMs: 15 * 60 * 1000, max: 100 })` nghĩa là tối đa 100 requests mỗi 15 phút cho mỗi IP. Có nhiều thuật toán khác nhau: fixed window (đơn giản nhất), sliding window (chính xác hơn), và token bucket (linh hoạt cho burst traffic). Trong production cần dùng Redis store thay vì memory store mặc định, vì nếu app chạy nhiều instances thì mỗi instance có bộ đếm riêng sẽ không chính xác, và nên cấu hình rate limit khác nhau cho từng loại endpoint — ví dụ login endpoint strict hơn nhiều so với endpoint đọc dữ liệu." },
  { id: 920, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Validation input trong Express dùng gì?", a: "Zod (TypeScript-first): `const schema = z.object({ email: z.string().email(), age: z.number().min(18) }); app.post('/users', (req, res) => { const result = schema.safeParse(req.body); if (!result.success) return res.status(400).json(result.error); })`. Alternatives: Joi, Yup, class-validator. Validate ở layer đầu tiên." },
  { id: 921, category: "Node.js", subcategory: "Node.js Deep", level: "advanced", q: "Caching strategy cho Node.js API?", a: "Caching cho Node.js API hoạt động theo nhiều tầng, mỗi tầng phục vụ mục đích khác nhau. Tầng in-memory dùng node-cache hoặc Map để cache dữ liệu truy cập thường xuyên ngay trong process, truy xuất cực nhanh nhưng mất khi restart. Tầng distributed dùng Redis để cache chia sẻ giữa nhiều server instances, phù hợp cho production. Pattern phổ biến nhất là cache-aside: kiểm tra cache trước, nếu miss thì query database rồi set vào cache với TTL, ví dụ cache key dạng `users:${id}:profile` với TTL 5 phút. Phần khó nhất là invalidation — có thể dùng TTL-based (tự hết hạn sau thời gian) hoặc event-based (khi data thay đổi thì xóa cache liên quan), và cần cẩn thận với cache stampede khi nhiều requests cùng miss cache đồng thời." },
  { id: 922, category: "Node.js", subcategory: "Node.js Deep", level: "advanced", q: "Error handling strategy cho production Node.js?", a: "Trong production Node.js, cần phân biệt rõ hai loại lỗi để xử lý khác nhau. Operational errors là lỗi dự kiến được như user gửi data sai (400), không có quyền (401), hoặc resource không tồn tại (404) — xử lý gracefully bằng cách trả HTTP status phù hợp và message rõ ràng. Programming errors là bugs thật sự như TypeError hay null reference — cách tốt nhất là để process crash rồi tự restart bằng PM2 hoặc Docker, vì app đang ở trạng thái không xác định và tiếp tục chạy có thể gây hậu quả nghiêm trọng hơn. Cần có global handlers `process.on('uncaughtException')` và `process.on('unhandledRejection')` để log lỗi trước khi crash, kết hợp logging library như Winston hoặc Pino với log levels (error, warn, info, debug) và format JSON để dễ parse bởi log aggregators." },
  { id: 923, category: "Node.js", subcategory: "Node.js Deep", level: "advanced", q: "Database connection pooling quan trọng thế nào?", a: "Tạo pool connections sẵn, tái sử dụng thay vì mở/đóng mỗi query. `new Pool({ max: 20, idleTimeoutMillis: 30000 })`. Không có pooling: mỗi request mở connection mới → chậm, exhaust DB connections. Prisma, Sequelize tự quản lý pool. Monitor: active/idle/waiting connections." },
  { id: 924, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Logging best practices cho Node.js production?", a: "Logging trong production cần có cấu trúc và chiến lược rõ ràng để debug hiệu quả khi có sự cố. Nên dùng Pino (nhanh nhất, gấp 5 lần Winston) hoặc Winston (linh hoạt hơn, nhiều transports), và format log dưới dạng JSON thay vì text thuần vì JSON dễ parse bởi log aggregators như ELK Stack, Datadog, hay CloudWatch. Phân chia log levels hợp lý: error cho lỗi cần xử lý ngay, warn cho tình huống bất thường nhưng chưa lỗi, info cho business events quan trọng, debug cho chi tiết kỹ thuật chỉ bật khi cần. Mỗi request nên có correlation ID (UUID) truyền qua tất cả services để trace toàn bộ luồng xử lý, và tuyệt đối không log sensitive data như passwords, tokens, hay thông tin cá nhân người dùng." },
  { id: 925, category: "Node.js", subcategory: "Node.js Deep", level: "intermediate", q: "Websocket với Socket.io trong Node.js dùng thế nào?", a: "Server: `const io = new Server(httpServer); io.on('connection', socket => { socket.on('message', data => { io.emit('message', data); }); });` Client: `const socket = io('http://localhost:3000'); socket.emit('message', 'hello');` Rooms cho group chat, namespaces cho tách concerns. Reconnection tự động." },
  { id: 926, category: "Node.js", subcategory: "Node.js Deep", level: "advanced", q: "Microservices communication patterns?", a: "Microservices giao tiếp với nhau qua hai cách chính: synchronous và asynchronous. Synchronous dùng REST (đơn giản, phổ biến) hoặc gRPC (nhanh hơn nhờ Protocol Buffers và HTTP/2, type-safe, phù hợp internal services). Asynchronous dùng message queue như RabbitMQ cho task queue đơn giản, Redis pub/sub cho real-time events, hoặc Kafka cho high-throughput event streaming. Pattern API Gateway đặt ở trước làm single entry point, xử lý routing, authentication, và rate limiting tập trung thay vì mỗi service tự làm. Circuit breaker pattern rất quan trọng: khi một service lỗi, tạm ngắt kết nối đến service đó thay vì tiếp tục gửi requests gây cascade failure — thư viện opossum cho Node.js hỗ trợ pattern này." },

]
