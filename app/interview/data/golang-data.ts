import type { QAItem } from '../interview-data'

export const GOLANG_DATA: QAItem[] = [
  // === Go: Cơ Bản (1001-1020) ===
  { id: 1001, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Go (Golang) là gì? Tại sao nên học Go?", a: "Go là ngôn ngữ lập trình do Google tạo ra (2009), biên dịch (compiled), statically typed, cú pháp đơn giản. Ưu điểm: tốc độ gần C/C++, concurrency mạnh (goroutines), binary đơn (không cần runtime), stdlib phong phú, phù hợp cho microservices, cloud tools, APIs." },
  { id: 1002, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Khai báo biến trong Go có mấy cách?", a: "3 cách: (1) `var name string = \"Go\"` (đầy đủ). (2) `var name = \"Go\"` (type inference). (3) `name := \"Go\"` (short declaration, chỉ trong function). `:=` phổ biến nhất. `var` dùng ở package level hoặc khi cần zero value. Go không có `let`/`const` như JS." },
  { id: 1003, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Go có những kiểu dữ liệu cơ bản nào?", a: "Go có các nhóm kiểu dữ liệu cơ bản: nhóm số gồm `int`, `int8/16/32/64`, `float32/64`, `complex64/128`; nhóm văn bản là `string` (immutable, mã hóa UTF-8); và `bool` cho giá trị logic true/false. Ngoài ra còn có `byte` (alias của uint8) dùng cho dữ liệu nhị phân, và `rune` (alias của int32) đại diện cho một ký tự Unicode. Mỗi kiểu đều có zero value mặc định khi khai báo mà không gán giá trị, ví dụ int là 0, string là chuỗi rỗng, bool là false, pointer là nil." },
  { id: 1004, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Zero value trong Go là gì?", a: "Giá trị mặc định khi khai báo biến không khởi tạo — Go đảm bảo mọi biến đều có giá trị hợp lệ (khác JS/undefined, Python/None). Cụ thể: int/float → 0, string → `\"\"` (rỗng), bool → false, pointer/slice/map/channel/interface → nil, struct → zero value của mỗi field. Ý nghĩa: code an toàn hơn, không cần check null/undefined, zero value thường là trạng thái hợp lý (ví dụ: counter bắt đầu từ 0)." },
  { id: 1005, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Hàm (function) trong Go khai báo thế nào?", a: "`func add(a int, b int) int { return a + b }`. Go hỗ trợ multiple return values: `func divide(a, b float64) (float64, error)`. Named return values: `func swap(a, b int) (x, y int) { return b, a }`. Functions là first-class citizens, có thể gán cho biến." },
  { id: 1006, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Multiple return values trong Go dùng khi nào?", a: "Pattern phổ biến nhất: trả (result, error). `val, err := strconv.Atoi(\"123\")`. Nếu không cần 1 giá trị, dùng `_` (blank identifier): `_, err := doSomething()`. Khác biệt lớn với JS/TS: Go không dùng try/catch mà handle error ngay tại chỗ." },
  { id: 1007, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Package và import trong Go hoạt động thế nào?", a: "Mỗi file Go thuộc 1 package (`package main`). `main` package + `func main()` = entry point. Import: `import \"fmt\"` hoặc grouped import. Exported names (public) viết hoa chữ cái đầu: `fmt.Println`. Lowercase = unexported (private trong package)." },
  { id: 1008, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Go có class không? Struct là gì?", a: "Go KHÔNG có class. Struct là kiểu dữ liệu composite: `type User struct { Name string; Age int }`. Methods gắn vào struct qua receiver: `func (u User) Hello() string { return u.Name }`. Composition thay vì inheritance: embed struct trong struct khác." },
  { id: 1009, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Pointer trong Go là gì? Khi nào dùng?", a: "`*int` là pointer type, `&x` lấy address, `*p` dereference. Dùng pointer khi: (1) muốn modify giá trị gốc (pass by reference). (2) struct lớn (tránh copy). (3) method cần modify receiver. Go không có pointer arithmetic như C. nil pointer gây panic." },
  { id: 1010, category: "Golang", subcategory: "Cơ Bản", level: "beginner", q: "Array và Slice trong Go khác nhau thế nào?", a: "Array: size cố định, value type `[5]int`. Slice: dynamic, reference type `[]int`. Slice dùng phổ biến hơn. `make([]int, 0, 10)` tạo slice capacity 10. `append(slice, item)` thêm phần tử. Slice là view lên underlying array." },

  // === Go: Intermediate (1011-1030) ===
  { id: 1011, category: "Golang", subcategory: "Collections", level: "intermediate", q: "Map trong Go hoạt động thế nào?", a: "`m := map[string]int{\"a\": 1}`. Check key tồn tại: `val, ok := m[\"key\"]`. Delete: `delete(m, \"key\")`. Map không thread-safe → dùng `sync.Map` hoặc mutex. Zero value là nil (phải make/literal trước khi dùng). Iteration order không đảm bảo." },
  { id: 1012, category: "Golang", subcategory: "Collections", level: "intermediate", q: "Range loop trong Go dùng thế nào?", a: "`for i, v := range slice { }` (index + value). `for k, v := range map { }` (key + value). `for i, c := range \"hello\" { }` (index + rune). Dùng `_` bỏ qua index/value: `for _, v := range items { }`. Range tạo copy của value, không phải reference." },
  { id: 1013, category: "Golang", subcategory: "Error Handling", level: "intermediate", q: "Error handling trong Go khác gì JS/Python?", a: "Go không có try/catch/throw. Errors là values: `func read() ([]byte, error)`. Check lỗi ngay: `if err != nil { return err }`. Custom errors: `fmt.Errorf(\"failed: %w\", err)` (wrapping). `errors.Is()`, `errors.As()` để check error chain. Explicit > implicit." },
  { id: 1014, category: "Golang", subcategory: "Error Handling", level: "intermediate", q: "defer, panic, recover trong Go dùng khi nào?", a: "Keyword `defer` dùng để đảm bảo một function sẽ được gọi khi function chứa nó return, thường dùng cho cleanup như đóng file, unlock mutex, và các deferred calls thực thi theo thứ tự LIFO (gọi sau chạy trước). `panic` dùng để dừng chương trình ngay lập tức khi gặp lỗi không thể khôi phục, ví dụ lỗi logic nghiêm trọng mà không nên tiếp tục chạy. `recover` chỉ hoạt động bên trong deferred function, dùng để bắt panic và xử lý thay vì để chương trình crash, theo pattern: `defer func() { if r := recover(); r != nil { log.Println(r) } }()`. Trong thực tế, hạn chế dùng panic/recover và ưu tiên trả error thông thường." },
  { id: 1015, category: "Golang", subcategory: "Interface", level: "intermediate", q: "Interface trong Go hoạt động thế nào?", a: "Interface = tập hợp method signatures. Implicit implementation: struct tự động satisfy interface nếu có đủ methods (không cần `implements` keyword). `type Writer interface { Write([]byte) (int, error) }`. Empty interface `interface{}` (hoặc `any`) chấp nhận mọi type." },
  { id: 1016, category: "Golang", subcategory: "Interface", level: "intermediate", q: "Empty interface (any) dùng khi nào?", a: "`any` (alias `interface{}`) chấp nhận mọi type, nhưng khác TS `any`: Go vẫn yêu cầu type assertion để dùng value. Dùng cho: generic containers, JSON parsing (`map[string]any`), function nhận nhiều types. Type assertion để lấy giá trị: `val, ok := i.(string)`. Go 1.18+ có generics thay thế nhiều use cases." },
  { id: 1017, category: "Golang", subcategory: "Interface", level: "intermediate", q: "Type assertion và type switch là gì?", a: "Type assertion: `s := i.(string)` (panic nếu sai type), `s, ok := i.(string)` (safe). Type switch: `switch v := i.(type) { case string: ... case int: ... default: ... }`. Dùng khi xử lý interface{}/any values. Tương tự instanceof trong JS." },
  { id: 1018, category: "Golang", subcategory: "Struct", level: "intermediate", q: "Struct embedding (composition) trong Go?", a: "Nhúng struct trong struct: `type Admin struct { User; Level int }`. Admin kế thừa methods và fields của User. Gọi trực tiếp: `admin.Name` thay vì `admin.User.Name`. Đây là composition, không phải inheritance. Có thể embed nhiều structs, interface cũng embed được." },
  { id: 1019, category: "Golang", subcategory: "Struct", level: "intermediate", q: "Pointer receiver vs value receiver khác nhau?", a: "Value receiver `func (u User) Name()`: nhận copy, không modify gốc. Pointer receiver `func (u *User) SetName(n string)`: modify struct gốc, không copy. Dùng pointer khi: modify state, struct lớn (tránh copy), consistency (nếu 1 method dùng pointer, nên tất cả dùng). " },
  { id: 1020, category: "Golang", subcategory: "Generics", level: "intermediate", q: "Generics trong Go (1.18+) hoạt động thế nào?", a: "Generics cho phép viết function và type hoạt động với nhiều kiểu dữ liệu mà không cần lặp code, ví dụ `func Map[T any, U any](s []T, f func(T) U) []U` nhận slice bất kỳ và trả về slice đã transform. Type parameters được khai báo trong dấu ngoặc vuông `[]`, kèm constraints để giới hạn kiểu được chấp nhận như `any`, `comparable`, hoặc custom interface constraints kiểu `type Number interface { int | float64 }`. Trước Go 1.18, muốn viết hàm generic phải dùng `interface{}` rồi type assertion, vừa mất type safety vừa verbose; generics giải quyết triệt để vấn đề này." },

  // === Go: Concurrency (1021-1040) ===
  { id: 1021, category: "Golang", subcategory: "Concurrency", level: "intermediate", q: "Goroutine là gì? Khác thread thế nào?", a: "Goroutine là lightweight thread do Go runtime quản lý. Khởi tạo: `go myFunc()`. Khác OS thread: goroutine stack default ~8KB, tự grow/shrink (thread ~1-8MB cố định), Go scheduler multiplex goroutines lên OS threads (M:N scheduling). Có thể chạy hàng triệu goroutines cùng lúc." },
  { id: 1022, category: "Golang", subcategory: "Concurrency", level: "intermediate", q: "Channel trong Go là gì? Dùng thế nào?", a: "Kênh giao tiếp giữa goroutines: `ch := make(chan int)`. Send: `ch <- 42`. Receive: `val := <-ch`. Unbuffered channel: block đến khi cả sender và receiver sẵn sàng. Buffered: `make(chan int, 10)` không block nếu buffer chưa đầy. Channel đảm bảo synchronization." },
  { id: 1023, category: "Golang", subcategory: "Concurrency", level: "intermediate", q: "Buffered vs unbuffered channel khác nhau?", a: "Unbuffered channel (`make(chan int)`) hoạt động đồng bộ: goroutine gửi dữ liệu sẽ bị block cho đến khi có goroutine khác sẵn sàng nhận, đảm bảo hai bên luôn gặp nhau tại thời điểm trao đổi. Buffered channel (`make(chan int, 5)`) có bộ đệm nên goroutine gửi chỉ bị block khi buffer đã đầy, cho phép sender và receiver hoạt động không đồng bộ. Buffered channel thường dùng trong các pattern như producer-consumer, rate limiting, hoặc semaphore để giới hạn số goroutine chạy đồng thời. Khi close channel bằng `close(ch)`, range loop sẽ tự động dừng sau khi đọc hết dữ liệu còn lại trong buffer." },
  { id: 1024, category: "Golang", subcategory: "Concurrency", level: "intermediate", q: "Select statement dùng để làm gì?", a: "`select { case v := <-ch1: ... case ch2 <- x: ... default: ... }` chờ multiple channel operations, chạy case sẵn sàng đầu tiên. Nếu nhiều case sẵn sàng → chọn random. `default` chạy nếu không case nào sẵn sàng (non-blocking). Dùng cho timeout: `case <-time.After(5*time.Second)`." },
  { id: 1025, category: "Golang", subcategory: "Concurrency", level: "intermediate", q: "WaitGroup dùng khi nào?", a: "`var wg sync.WaitGroup`. `wg.Add(n)` đăng ký n goroutines. `wg.Done()` (trong goroutine, thường defer). `wg.Wait()` block đến khi counter = 0. Dùng khi cần đợi nhiều goroutines hoàn thành trước khi tiếp tục. Đơn giản hơn channel cho fan-out/fan-in." },
  { id: 1026, category: "Golang", subcategory: "Concurrency", level: "advanced", q: "Mutex và RWMutex dùng khi nào?", a: "`sync.Mutex`: Lock()/Unlock() cho exclusive access (1 goroutine tại 1 thời điểm). `sync.RWMutex`: RLock() cho nhiều readers đồng thời, Lock() cho exclusive write. Dùng khi shared state giữa goroutines. Luôn `defer mu.Unlock()`. Tránh deadlock: lock order nhất quán." },
  { id: 1027, category: "Golang", subcategory: "Concurrency", level: "advanced", q: "Context package dùng để làm gì?", a: "Package `context` cung cấp cơ chế truyền deadline, tín hiệu hủy (cancellation), và các giá trị request-scoped xuyên suốt chuỗi goroutine trong một request. Ví dụ `context.WithTimeout(parent, 5*time.Second)` tạo context tự động hủy sau 5 giây, còn `context.WithCancel(parent)` cho phép hủy thủ công khi không cần kết quả nữa. Trong HTTP server, mỗi request đều mang theo context qua `r.Context()`, giúp tự động hủy các thao tác downstream (database query, API call) khi client ngắt kết nối. Nguyên tắc quan trọng: luôn truyền context như tham số đầu tiên của function, không bao giờ lưu context vào struct." },
  { id: 1028, category: "Golang", subcategory: "Concurrency", level: "advanced", q: "Worker pool pattern trong Go?", a: "Tạo fixed number goroutines (workers) đọc jobs từ channel: `func worker(jobs <-chan Job, results chan<- Result) { for j := range jobs { results <- process(j) } }`. Main: gửi jobs vào channel, workers xử lý parallel. Giới hạn concurrency, tránh tạo quá nhiều goroutines." },
  { id: 1029, category: "Golang", subcategory: "Concurrency", level: "advanced", q: "Race condition trong Go phát hiện và fix thế nào?", a: "Phát hiện: `go run -race main.go` (race detector). Nguyên nhân: nhiều goroutines đọc/ghi shared variable không sync. Fix: (1) Mutex/RWMutex. (2) Channel (share by communicating). (3) sync/atomic cho simple counters. (4) Redesign để tránh shared state. Go philosophy: 'Don't communicate by sharing memory; share memory by communicating.'" },
  { id: 1030, category: "Golang", subcategory: "Concurrency", level: "advanced", q: "Errgroup dùng khi nào?", a: "`golang.org/x/sync/errgroup`: chạy nhiều goroutines, trả lỗi đầu tiên, cancel tất cả khi 1 lỗi. `g, ctx := errgroup.WithContext(ctx); g.Go(func() error { ... }); if err := g.Wait(); err != nil { ... }`. Tốt hơn WaitGroup khi cần error propagation và cancellation." },

  // === Go: Web & API (1031-1045) ===
  { id: 1031, category: "Golang", subcategory: "Web & API", level: "beginner", q: "HTTP server cơ bản trong Go viết thế nào?", a: "`http.HandleFunc(\"/hello\", func(w http.ResponseWriter, r *http.Request) { fmt.Fprintf(w, \"Hello!\") }); http.ListenAndServe(\":8080\", nil)`. Stdlib đủ mạnh cho production. Không bắt buộc framework. `http.ServeMux` là default router." },
  { id: 1032, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "Gin framework là gì? Khác net/http thế nào?", a: "Gin là HTTP web framework phổ biến nhất trong hệ sinh thái Go, được thiết kế tập trung vào hiệu năng với routing dựa trên radix tree, nhanh hơn đáng kể so với `http.ServeMux` mặc định. So với stdlib net/http, Gin cung cấp sẵn nhiều tính năng thiết yếu: middleware chain, JSON binding và validation tự động, error handling tập trung, và group routes để tổ chức API gọn gàng. Cách dùng cơ bản: `r := gin.Default(); r.GET(\"/users/:id\", getUser); r.Run()`, trong đó `gin.Default()` đã bao gồm sẵn middleware logger và recovery. Các framework thay thế đáng chú ý gồm Echo, Fiber (dựa trên fasthttp), và Chi (tương thích net/http)." },
  { id: 1033, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "Middleware trong Go HTTP server viết thế nào?", a: "Middleware trong Go là một wrapper function nhận handler và trả về handler mới, cho phép chèn logic trước hoặc sau khi xử lý request, ví dụ: `func authMiddleware(next http.Handler) http.Handler { return http.HandlerFunc(func(w, r) { if !isAuth(r) { http.Error(w, \"Unauthorized\", 401); return }; next.ServeHTTP(w, r) }) }`. Để chain nhiều middleware, ta lồng chúng lại: `http.Handle(\"/api\", logging(auth(apiHandler)))`, request sẽ đi qua từng lớp middleware từ ngoài vào trong. Trong Gin framework, middleware được đăng ký đơn giản hơn qua `r.Use(authMiddleware())` và áp dụng cho toàn bộ router hoặc từng route group." },
  { id: 1034, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "JSON encode/decode trong Go thế nào?", a: "Go sử dụng package `encoding/json` để làm việc với JSON: `json.Marshal(struct)` chuyển struct thành `[]byte` JSON, còn `json.Unmarshal(data, &struct)` parse JSON ngược lại thành struct. Struct tags quyết định cách ánh xạ field sang JSON key, ví dụ `type User struct { Name string \\`json:\"name\"\\`; Age int \\`json:\"age,omitempty\"\\` }`, trong đó `omitempty` sẽ bỏ qua field có zero value khi encode. Khi viết HTTP response, nên dùng `json.NewEncoder(w).Encode(data)` để stream trực tiếp thay vì marshal rồi write, vừa tiết kiệm bộ nhớ vừa gọn code hơn." },
  { id: 1035, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "GORM ORM trong Go dùng thế nào?", a: "GORM là ORM phổ biến nhất trong Go, cung cấp API trực quan để thao tác database: `db.Create(&user)` để insert, `db.First(&user, 1)` để query theo primary key, và `db.Where(\"age > ?\", 18).Find(&users)` cho query có điều kiện. GORM hỗ trợ auto migration (`db.AutoMigrate(&User{})`) để tự tạo/cập nhật bảng theo struct definition, cùng với các associations như BelongsTo, HasMany, ManyToMany và hooks như BeforeCreate, AfterUpdate để chạy logic trước/sau thao tác. Tuy nhiên với các query phức tạp hoặc cần kiểm soát performance chặt, nhiều team chọn dùng sqlx (raw SQL kết hợp auto scanning) hoặc ent (Facebook, code generation) thay thế." },
  { id: 1036, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "Database connection trong Go best practices?", a: "`sql.Open()` trả connection pool (không phải single connection). Set: `db.SetMaxOpenConns(25)`, `db.SetMaxIdleConns(5)`, `db.SetConnMaxLifetime(5*time.Minute)`. KHÔNG `defer db.Close()` mỗi request vì sql.DB là long-lived pool — chỉ close khi app shutdown. Prepared statements cho queries lặp lại. Context timeout cho queries: `db.QueryContext(ctx, query)`." },
  { id: 1037, category: "Golang", subcategory: "Web & API", level: "intermediate", q: "Go modules là gì? go.mod hoạt động thế nào?", a: "Go modules là hệ thống quản lý dependency chính thức của Go, khởi tạo bằng `go mod init module-name` để tạo file go.mod chứa tên module và danh sách dependencies. Khi cần thêm thư viện, dùng `go get package@version`, và `go mod tidy` để tự động dọn dẹp các dependency không còn sử dụng cũng như thêm các dependency bị thiếu. File go.sum đóng vai trò lock file (tương tự package-lock.json trong Node.js), lưu checksum chính xác của từng dependency để đảm bảo build reproducible. Go modules còn hỗ trợ replace directive để trỏ sang local module khi phát triển, và sử dụng GOPROXY để tải dependency qua proxy server nhằm tăng tốc và đảm bảo tính sẵn có." },
  { id: 1038, category: "Golang", subcategory: "Web & API", level: "advanced", q: "gRPC trong Go dùng khi nào?", a: "RPC framework dùng Protocol Buffers (protobuf). Nhanh hơn REST (binary encoding, HTTP/2). Dùng khi: microservices internal communication, streaming (bidirectional), cần type safety chặt. Define service trong .proto file → `protoc` generate Go code. Hỗ trợ: unary, server streaming, client streaming, bidirectional." },
  { id: 1039, category: "Golang", subcategory: "Web & API", level: "advanced", q: "Graceful shutdown trong Go server thế nào?", a: "`srv := &http.Server{Addr: \":8080\"}; go srv.ListenAndServe()`. Signal handler: `quit := make(chan os.Signal, 1); signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM); <-quit`. Shutdown: `ctx, cancel := context.WithTimeout(ctx, 30*time.Second); srv.Shutdown(ctx)`. Đợi requests đang xử lý hoàn thành." },
  { id: 1040, category: "Golang", subcategory: "Web & API", level: "advanced", q: "Rate limiter implement trong Go?", a: "Để implement rate limiting trong Go, cách chuẩn là dùng package `golang.org/x/time/rate` với token bucket algorithm: `limiter := rate.NewLimiter(rate.Every(time.Second), 10)` tạo limiter cho phép 10 request mỗi giây. Trong HTTP middleware, kiểm tra `if !limiter.Allow()` và trả về status 429 Too Many Requests nếu vượt giới hạn. Để giới hạn theo từng client (per-IP), cần dùng `map[string]*rate.Limiter` kết hợp với mutex để quản lý limiter riêng cho mỗi IP, đồng thời nên dọn dẹp các limiter cũ theo định kỳ để tránh memory leak. Trong môi trường production nhiều server, nên dùng Redis-based distributed rate limiter để đảm bảo giới hạn nhất quán across tất cả instances." },

  // === Go: Testing & Tooling (1041-1050) ===
  { id: 1041, category: "Golang", subcategory: "Testing", level: "intermediate", q: "Testing trong Go viết thế nào?", a: "File `_test.go`, function `TestXxx(t *testing.T)`. Run: `go test ./...`. Assert: `if got != want { t.Errorf(\"got %d, want %d\", got, want) }` (không có built-in assert). Table-driven tests phổ biến: `tests := []struct{ input, want string }{}; for _, tt := range tests { t.Run(tt.name, func(t *testing.T) { ... }) }`." },
  { id: 1042, category: "Golang", subcategory: "Testing", level: "intermediate", q: "Table-driven tests trong Go là gì?", a: "Pattern test nhiều cases bằng slice of structs: `cases := []struct{ name string; input int; want int }{ {\"positive\", 5, 25}, {\"zero\", 0, 0} }; for _, tc := range cases { t.Run(tc.name, func(t *testing.T) { got := Square(tc.input); if got != tc.want { t.Errorf(...) } }) }`. DRY, dễ thêm cases." },
  { id: 1043, category: "Golang", subcategory: "Testing", level: "intermediate", q: "Benchmark testing trong Go?", a: "`func BenchmarkXxx(b *testing.B) { for i := 0; i < b.N; i++ { myFunc() } }`. Run: `go test -bench=.`. Output: ns/op, B/op, allocs/op. `b.ResetTimer()` bỏ qua setup time. So sánh: `benchstat old.txt new.txt`. Dùng để optimize performance-critical code." },
  { id: 1044, category: "Golang", subcategory: "Testing", level: "intermediate", q: "Mock/stub trong Go thế nào?", a: "Go khuyến khích mocking dựa trên interface: ta define một interface nhỏ cho dependency cần mock, sau đó tạo mock struct implement interface đó với behavior tùy chỉnh, ví dụ `type MockDB struct { GetFunc func(id int) (*User, error) }` rồi gọi `m.GetFunc(id)` trong method Get. Cách này tận dụng implicit interface của Go nên không cần sửa code production, chỉ cần đảm bảo mock struct có đủ methods. Các thư viện phổ biến như testify/mock, gomock, hay mockery (tự động generate mock từ interface) giúp giảm boilerplate khi có nhiều methods cần mock. Nguyên tắc quan trọng là chỉ mock ở boundary (database, external API) và tránh mock quá nhiều layer vì sẽ làm test mất ý nghĩa." },
  { id: 1045, category: "Golang", subcategory: "Tooling", level: "beginner", q: "Go toolchain có những công cụ nào?", a: "Go đi kèm bộ toolchain rất mạnh ngay từ khi cài đặt: `go build` để compile thành binary, `go run` để compile và chạy luôn, `go test` để chạy unit test và benchmark, và `go fmt` để format code theo chuẩn thống nhất (bắt buộc trong hầu hết project). Về chất lượng code, `go vet` phát hiện các lỗi tiềm ẩn mà compiler không bắt được, còn `golangci-lint` là công cụ linting tổng hợp chạy hàng chục linter cùng lúc. Ngoài ra còn có `go mod` cho quản lý dependency, `go generate` cho code generation, `go doc` để xem documentation, và `go tool pprof` để profiling hiệu năng ứng dụng." },
  { id: 1046, category: "Golang", subcategory: "Tooling", level: "intermediate", q: "Profiling Go application thế nào?", a: "Go có hỗ trợ profiling tích hợp sẵn thông qua package `net/http/pprof`: chỉ cần import `_ \"net/http/pprof\"` là server tự expose các endpoint profiling tại `/debug/pprof/`. Để phân tích CPU, dùng `go test -cpuprofile=cpu.prof` rồi mở bằng `go tool pprof cpu.prof`; tương tự với memory profiling dùng flag `-memprofile=mem.prof` để tìm các hàm allocate nhiều bộ nhớ. Công cụ pprof hỗ trợ xem flame graph trực quan qua `pprof -http=:8080`, giúp nhanh chóng nhận diện hot functions, excessive allocations, và goroutine leaks. Ngoài ra `go tool trace` cho phép xem chi tiết execution trace theo timeline, hữu ích khi debug vấn đề concurrency và latency." },

  // === Go: Advanced Patterns (1047-1060) ===
  { id: 1047, category: "Golang", subcategory: "Patterns", level: "advanced", q: "Functional options pattern trong Go?", a: "Functional options là pattern tạo API khởi tạo object linh hoạt bằng cách define type `Option func(*Server)` và các hàm trả về Option như `func WithPort(p int) Option { return func(s *Server) { s.port = p } }`. Khi sử dụng, ta gọi `NewServer(WithPort(8080), WithTimeout(30*time.Second))`, mỗi option tự apply cấu hình lên server instance. Pattern này giúp API sạch sẽ, dễ mở rộng (thêm option mới không break code cũ), và backwards compatible, thay thế hiệu quả cho config struct lớn với nhiều optional fields mà caller phải điền zero value cho những field không quan tâm." },
  { id: 1048, category: "Golang", subcategory: "Patterns", level: "advanced", q: "Builder pattern trong Go?", a: "`type QueryBuilder struct { table string; wheres []string }; func (qb *QueryBuilder) Where(cond string) *QueryBuilder { qb.wheres = append(qb.wheres, cond); return qb }; func (qb *QueryBuilder) Build() string { ... }`. Method chaining: `qb.Where(\"age > 18\").Where(\"active = true\").Build()`. Dùng cho complex object construction." },
  { id: 1049, category: "Golang", subcategory: "Patterns", level: "advanced", q: "Dependency injection trong Go thế nào?", a: "Dependency injection trong Go thực hiện chủ yếu qua constructor injection: function khởi tạo nhận interface làm tham số, ví dụ `func NewUserService(repo UserRepository) *UserService`, giúp UserService không phụ thuộc vào implementation cụ thể mà chỉ phụ thuộc vào contract (interface). Triết lý Go ưu tiên sự tường minh nên phần lớn project không cần DI framework, chỉ cần truyền dependency thủ công tại main function; tuy nhiên với ứng dụng lớn nhiều dependency lồng nhau, có thể dùng wire (Google, compile-time) hoặc fx (Uber, runtime). Interface nên giữ nhỏ (1-3 methods) theo nguyên tắc Interface Segregation, và khi viết test chỉ cần inject mock implementation vào constructor là xong." },
  { id: 1050, category: "Golang", subcategory: "Patterns", level: "advanced", q: "Singleton pattern trong Go?", a: "`var instance *DB; var once sync.Once; func GetDB() *DB { once.Do(func() { instance = &DB{...} }); return instance }`. `sync.Once` đảm bảo chỉ chạy 1 lần dù nhiều goroutines gọi đồng thời. Thread-safe. Dùng cho: DB connection, config, logger." },
  { id: 1051, category: "Golang", subcategory: "Patterns", level: "advanced", q: "Pipeline pattern với channels?", a: "Chain stages qua channels: `func gen(nums ...int) <-chan int` → `func square(in <-chan int) <-chan int` → `func print(in <-chan int)`. Mỗi stage là goroutine, data chảy qua channels. Fan-out: nhiều goroutines đọc cùng channel. Fan-in: merge nhiều channels thành 1. Dùng cho data processing pipelines." },
  { id: 1052, category: "Golang", subcategory: "Production", level: "advanced", q: "Go production best practices?", a: "Khi đưa ứng dụng Go lên production, cần đảm bảo structured logging (dùng slog, zap, hoặc zerolog) để log dạng JSON dễ parse bởi log aggregators, graceful shutdown để không mất request đang xử lý, và health check endpoints cho load balancer biết trạng thái service. Về configuration, nên đọc từ environment variables (dùng viper hoặc envconfig), Dockerfile dùng multi-stage build với scratch hoặc distroless base image để giảm attack surface và size xuống còn 5-15MB. Chất lượng code được đảm bảo bằng race detector (`go test -race`) trong CI pipeline và golangci-lint để catch lỗi sớm. Cuối cùng, luôn propagate context xuyên suốt request chain, cấu hình connection pooling hợp lý cho database, và expose metrics qua Prometheus để monitoring hiệu năng." },
  { id: 1053, category: "Golang", subcategory: "Production", level: "advanced", q: "Docker image cho Go app tối ưu thế nào?", a: "Cách tối ưu nhất là dùng multi-stage build: stage đầu tiên sử dụng image `golang:1.22-alpine` để compile source thành binary, stage thứ hai chỉ dùng `scratch` (image rỗng hoàn toàn) hoặc `gcr.io/distroless/static` rồi copy binary vào, cho ra final image chỉ 5-15MB thay vì hơn 1GB của golang base image. Để binary chạy được trên scratch image, cần set `CGO_ENABLED=0` khi build để tạo static binary không phụ thuộc vào thư viện C bên ngoài. Nên tách layer `COPY go.mod go.sum` và `RUN go mod download` riêng trước khi copy source code, để Docker cache lại dependency layer và chỉ rebuild khi dependency thay đổi, giúp tăng tốc build đáng kể." },
  { id: 1054, category: "Golang", subcategory: "Production", level: "intermediate", q: "Structured logging trong Go dùng gì?", a: "Từ Go 1.21, stdlib cung cấp package `log/slog` cho structured logging với cú pháp `slog.Info(\"user created\", \"id\", user.ID, \"email\", user.Email)`, output ra JSON có cấu trúc rõ ràng thay vì chuỗi text khó parse. Slog hỗ trợ các mức log chuẩn (Debug, Info, Warn, Error) và cho phép tùy chỉnh handler để thay đổi format hoặc destination output. Trước khi có slog, cộng đồng Go dùng zap (Uber, tối ưu hiệu năng cao nhất) hoặc zerolog (zero allocation) cho structured logging; cả hai vẫn phổ biến trong production vì có thêm nhiều tính năng nâng cao. Ưu điểm lớn nhất của structured logging là log dạng key-value pairs dễ dàng được index và query bởi các log aggregator như ELK Stack, Datadog, hay Grafana Loki." },
  { id: 1055, category: "Golang", subcategory: "Production", level: "intermediate", q: "Go vs Node.js khi nào nên chọn Go?", a: "Chọn Go khi: CPU-intensive tasks, high concurrency (100K+ connections), microservices, CLI tools, system programming, cần type safety mạnh. Chọn Node.js khi: full-stack JS, rapid prototyping, npm ecosystem, real-time (Socket.io), team quen JS. Go nhanh hơn ~10-50x cho compute, Node.js ecosystem lớn hơn." },
]
